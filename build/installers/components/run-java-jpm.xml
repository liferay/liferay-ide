<runProgram>
    <program>${java_executable}</program>
    <programArguments>-jar "${installdir}${platform_path_separator}biz.aQute.jpm.run.jar" -u init --jvmlocation "${java_executable}"</programArguments>
</runProgram>